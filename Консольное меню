using System;
using System.Collections.Generic;
using System.Globalization;
using System.Linq;
using System.Text;
using System.Threading.Tasks;

namespace CSLight
{
    internal class Program
    {
        static void Main(string[] args)
        {
            const string SetNameCommand = "1";
            const string ChangeConsoleColorCommand = "2";
            const string ChangeFontColorCommand = "3";
            const string SetPasswordCommand = "4";
            const string EscCommand = "5";

            string userInput;
            string userName;
            string userPassword;
            string seeYouLater = "До свидание!";

            Console.WriteLine("Данное приложение обрабатывает следующие команды: 1 Установить имя пользователя." +
                " 2 Сменить цвет консоли. 3 Сменить цвет шрифта. 4 Установить пароль. 5 Выйти из программы");
            Console.WriteLine("Выбрав команду, напишите ее номер.");
            userInput = Console.ReadLine();

            while (userInput != seeYouLater)
            {
                switch (userInput)
                {
                    case SetNameCommand:
                        Console.Write("Введите свое имя: ");
                        userName = Console.ReadLine();
                        Console.WriteLine("Вас зовут: " + userName);

                        Console.WriteLine("Введите номер команды: ");
                        userInput = Console.ReadLine();
                        break;

                    case ChangeConsoleColorCommand:
                        Console.BackgroundColor = ConsoleColor.Red;
                        Console.WriteLine("Цвет изменен.");

                        Console.WriteLine("Введите номер команды: ");
                        userInput = Console.ReadLine();
                        break;

                    case ChangeFontColorCommand:
                        Console.ForegroundColor = ConsoleColor.DarkYellow;
                        Console.WriteLine("Цвет изменен.");

                        Console.WriteLine("Введите номер команды: ");
                        userInput = Console.ReadLine();
                        break;

                    case SetPasswordCommand:
                        Console.WriteLine("Создайте пароль: ");
                        userPassword = Convert.ToString(Console.ReadLine());
                        Console.WriteLine("Ваш пароль: " + userPassword);

                        Console.WriteLine("Введите номер команды: ");
                        userInput = Console.ReadLine();
                        break;

                    case EscCommand:
                        Console.WriteLine(seeYouLater);
                        userInput = seeYouLater;
                        break;

                    default:
                        Console.WriteLine("Я не знаю такой команды. Попропуй другую.");
                        userInput = Console.ReadLine();
                        break;
                }
            }
        }
    }
}
